[project]
name = "trustee-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "kubernetes>=35.0.0",
    "mcp>=1.27.0",
    "pyyaml>=6.0.3",
    "veritas>=0.1.12",
]
